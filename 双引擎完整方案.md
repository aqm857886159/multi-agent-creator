# 双引擎选题雷达：完整技术方案

## 🎯 你的核心逻辑（非常棒！）

你提出的是一个**双引擎并行**策略，这正是当前项目设计的初衷，但需要完善实现：

```
引擎 1: 头部博主监控（纵向异常检测）
   ↓
发现顶级博主 → 定点监控 → 找出"超常发挥"的内容

引擎 2: 关键词搜索（横向对比）
   ↓
广泛搜索关键词 → 横向对比 → 找出"爆款黑马"
```

---

## 📊 深度分析：你的逻辑 vs 当前实现

### 你的引擎 1：头部博主监控

**你的逻辑**：
```
1. Web 搜索 "best AI channels 2025"
2. 找到推荐文章 → 提取头部博主
3. 监控这些博主的所有内容
4. 对比这个博主的历史数据
5. 找出"显著高于其他内容的主题"（超常发挥）
```

**当前实现的对应部分**：
- ✅ 已有：`monitor_items` 分组（filter.py 第 29-30 行）
- ✅ 已有：纵向对比逻辑（filter.py 第 38-66 行）
  ```python
  # 策略 A: 监控模式 (纵向对比)
  if item.author_avg_views > 0:
      ratio = item.view_count / item.author_avg_views
      if ratio > 1.2:  # 比平时好 20%
          valid_items.append(item)
  ```
- ❌ 缺失：**自动发现头部博主的完整流程**
- ❌ 缺失：**持久化博主列表**

---

### 你的引擎 2：关键词搜索

**你的逻辑**：
```
1. 搜索一个领域的关键词（如 "AI News 2025"）
2. 找到多篇不同博主的内容
3. 横向对比这些内容的数据
4. 找出"爆款"（远超中位数的内容）
```

**当前实现的对应部分**：
- ✅ 已有：`hunter_items` 分组（filter.py 第 32-34 行）
- ✅ 已有：横向对比逻辑（filter.py 第 68-106 行）
  ```python
  # 策略 B: 猎杀模式 (横向对比)
  median_views = statistics.median(views)
  is_view_outlier = (item.view_count > median_views * 1.5)
  if is_view_outlier:
      valid_items.append(item)
  ```
- ✅ 已有：`youtube_search`, `bilibili_search` 工具
- ⚠️ 问题：**关键词设计不够精准**（直接用领域名搜索）

---

## 🔍 关键发现：项目已有双引擎基础！

**好消息**：你的逻辑在代码里其实已经有基础实现了！

看 `filter.py` 第 27-36 行：
```python
# 监控类：包括所有 *_monitor 工具
monitor_items = [...]  # → 你的引擎 1

# 搜索类：包括所有 *_search 工具
hunter_items = [...]   # → 你的引擎 2
```

**但有 3 个关键缺陷**：

### 缺陷 1：缺少"发现头部博主"的自动化流程
- 现在：手动在 `settings.yaml` 配置白名单
- 理想：Web 搜索自动发现

### 缺陷 2：Harvest 机制是"被动发现"，不是"主动搜索"
- 现在：搜索时碰到什么频道，就收集什么频道
- 理想：专门搜索"博主推荐文章"，提取顶级博主

### 缺陷 3：两个引擎没有明确分工和优先级
- 现在：随机混合，Harvest 会"绑架"决策
- 理想：先引擎 1（发现博主），再引擎 2（关键词搜索），最后对比

---

## 🎨 完整技术方案

### 阶段架构

```
┌─────────────────────────────────────────────────────┐
│ 阶段 0: 初始化                                       │
│ - 加载白名单博主（如果有）                           │
│ - 用户输入目标领域                                   │
└─────────────────┬───────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────────────────┐
│ 🔴 阶段 1: 引擎启动（搜索词设计）                    │
│                                                      │
│ KeywordDesigner 节点:                               │
│ - 输入: target_domains ["AI News", "Python"]        │
│ - 输出:                                              │
│   * discovery_queries (发现博主用)                  │
│     ["best AI YouTube channels 2025",              │
│      "顶级AI博主推荐 2025"]                          │
│   * content_queries (搜索内容用)                    │
│     ["AI news November 2025",                       │
│      "Python 3.12 features"]                        │
└─────────────────┬───────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────────────────┐
│ 🟠 阶段 2A: 引擎 1 准备（发现头部博主）              │
│                                                      │
│ 2.1 Web 搜索发现博主:                                │
│     web_search(discovery_queries)                   │
│     → 找到推荐文章 12 篇                             │
│                                                      │
│ 2.2 提取博主信息:                                    │
│     InfluencerExtractor 节点                        │
│     → 从文章中提取博主列表                           │
│     → 输出: [                                        │
│         {name: "AI Explained", platform: "youtube"},│
│         {name: "李永乐老师", platform: "bilibili"}  │
│       ]                                              │
│                                                      │
│ 2.3 搜索博主内容（建立基线）:                        │
│     youtube_search "AI Explained"                   │
│     bilibili_search "李永乐老师"                     │
│     → 获取每个博主的代表作 5-8 条                    │
│     → 计算 author_avg_views（平均播放量）           │
└─────────────────┬───────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────────────────┐
│ 🟡 阶段 2B: 引擎 2 执行（关键词搜索）                │
│                                                      │
│ youtube_search(content_queries)                     │
│ bilibili_search(content_queries)                    │
│ → 广泛搜索，采集多个博主的内容                       │
│ → 建立横向对比池                                     │
└─────────────────┬───────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────────────────┐
│ 🟢 阶段 3: 引擎 1 深入（监控头部博主）               │
│                                                      │
│ youtube_monitor(发现的博主频道)                      │
│ bilibili_monitor(发现的博主ID)                       │
│ → 获取这些博主的最新内容 10-15 条                    │
│ → 用于纵向对比                                       │
└─────────────────┬───────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────────────────┐
│ 🔵 阶段 4: 双引擎筛选（核心！）                      │
│                                                      │
│ Filter 节点 (已有，需要增强):                        │
│                                                      │
│ ┌──────────────────────────────────────────────┐   │
│ │ 引擎 1 输出: 监控数据（纵向异常）             │   │
│ │ - 来源: youtube_monitor, bilibili_monitor    │   │
│ │ - 逻辑: 比平时数据好 20%                      │   │
│ │ - 标签: "头部博主超常发挥"                    │   │
│ │ - 得分: 80-100 分                             │   │
│ └──────────────────────────────────────────────┘   │
│                                                      │
│ ┌──────────────────────────────────────────────┐   │
│ │ 引擎 2 输出: 搜索数据（横向爆款）             │   │
│ │ - 来源: youtube_search, bilibili_search      │   │
│ │ - 逻辑: 超过中位数 1.5 倍                     │   │
│ │ - 标签: "赛道黑马"                            │   │
│ │ - 得分: 60-90 分                              │   │
│ └──────────────────────────────────────────────┘   │
│                                                      │
│ 合并排序 → Top 10                                   │
└─────────────────┬───────────────────────────────────┘
                  ↓
┌─────────────────────────────────────────────────────┐
│ 阶段 5: 选题策划                                     │
│                                                      │
│ Architect 节点:                                     │
│ - 输入: Top 10 精选素材（含双引擎标签）             │
│ - 分析: 哪些是头部博主的新话题？                     │
│         哪些是横向对比的爆款？                       │
│ - 输出: 3 个选题方案                                 │
└─────────────────────────────────────────────────────┘
```

---

## 🔧 具体实现方案

### 方案 A：最小改动（利用现有代码）⭐⭐⭐⭐⭐

**核心思路**：现有代码已经有双引擎基础，只需要补充"发现博主"流程

#### 需要新增的功能

1. **搜索词设计节点**（新增）
   ```python
   # nodes/keyword_designer.py
   def run_keyword_designer(state):
       # 为目标领域设计 2 类搜索词
       return {
           "discovery_queries": [...],  # 发现博主
           "content_queries": [...]     # 搜索内容
       }
   ```

2. **博主提取节点**（新增）
   ```python
   # nodes/influencer_extractor.py
   def run_influencer_extractor(state):
       # 从 web_search 结果中提取博主信息
       # 输出: discovered_influencers
   ```

3. **修改 Planner 的决策逻辑**（修改）
   ```python
   # nodes/planner.py
   def _deterministic_plan_v2(state):
       # 阶段 1: 如果有 discovery_queries，执行 web_search
       if state.discovery_queries and not state.web_search_done:
           return web_search(discovery_queries)

       # 阶段 2: 如果有 discovered_influencers，搜索他们的内容
       if state.discovered_influencers and not state.influencer_search_done:
           return search_influencer_content(...)

       # 阶段 3: 执行 content_queries（关键词搜索）
       if state.content_queries and not state.content_search_done:
           return youtube_search(content_queries)

       # 阶段 4: 监控发现的博主
       if state.pending_monitors:
           return youtube_monitor(...)
   ```

4. **增强 Filter 的输出标签**（修改）
   ```python
   # nodes/filter.py
   # 已有的监控逻辑，增加标签
   item.raw_data["engine"] = "engine1"  # 引擎 1
   item.raw_data["detection_type"] = "vertical_anomaly"  # 纵向异常

   # 已有的搜索逻辑，增加标签
   item.raw_data["engine"] = "engine2"  # 引擎 2
   item.raw_data["detection_type"] = "horizontal_outlier"  # 横向爆款
   ```

#### 改动文件清单

| 文件 | 改动类型 | 工作量 |
|------|---------|--------|
| `nodes/keyword_designer.py` | 新增 | 100 行 |
| `nodes/influencer_extractor.py` | 新增 | 150 行 |
| `nodes/planner.py` | 修改 | +50 行 |
| `nodes/filter.py` | 修改 | +10 行（加标签） |
| `core/state.py` | 修改 | +15 行（新字段） |
| `core/graph.py` | 修改 | +20 行（新节点） |

**总工作量**：约 2-3 小时

---

### 方案 B：完全重构（推倒重来）⭐⭐⭐

**不推荐**，因为现有代码已经很好了，只是缺少"发现博主"的自动化。

---

## 💡 关键设计细节

### 1. 搜索词设计策略

**引擎 1 搜索词**（发现博主）：
```
英文:
- "best {领域} YouTube channels 2025"
- "top {领域} influencers to follow"
- "must-watch {领域} creators"

中文:
- "顶级{领域}博主推荐 2025"
- "必看的{领域}UP主"
- "{领域}头部创作者"
```

**引擎 2 搜索词**（搜索内容）：
```
英文:
- "{领域} {时间} latest"
- "{领域} news {月份}"

中文:
- "{领域}最新动态"
- "{领域} {月份}"
```

---

### 2. 博主信息提取

**从 Web 搜索结果中提取**：

```python
# 示例文章标题
"2025年必看的10个AI频道"
"这些YouTube频道最适合学Python"

# LLM 提取
{
  "influencers": [
    {
      "name": "AI Explained",
      "platform": "youtube",
      "identifier": "@AIExplained",
      "mention_count": 3  # 在多篇文章中出现
    },
    {
      "name": "李永乐老师",
      "platform": "bilibili",
      "identifier": "UIDxxxx",
      "mention_count": 2
    }
  ]
}
```

**优先级排序**：
- 提及次数多的优先
- 同时出现在多篇文章中的优先

---

### 3. 双引擎数据标注

**在 Filter 中增加标签**：

```python
# 引擎 1: 监控数据
item.raw_data["engine"] = "engine1"
item.raw_data["detection_type"] = "vertical_anomaly"  # 纵向异常
item.raw_data["analysis_note"] = "头部博主超常发挥"

# 引擎 2: 搜索数据
item.raw_data["engine"] = "engine2"
item.raw_data["detection_type"] = "horizontal_outlier"  # 横向爆款
item.raw_data["analysis_note"] = "赛道黑马"
```

**在 Architect 中利用标签**：

```python
# 策划时可以区分
engine1_items = [i for i in top_items if i.raw_data.get("engine") == "engine1"]
engine2_items = [i for i in top_items if i.raw_data.get("engine") == "engine2"]

# 选题方向
# - 引擎 1 的素材：关注"话题趋势"（头部博主都在讲什么）
# - 引擎 2 的素材：关注"内容形式"（什么样的内容容易爆）
```

---

## 📊 数据流示例

### 输入
```
target_domains: ["AI News", "Python Tutorials"]
```

### 阶段 1: 搜索词设计
```json
{
  "discovery_queries": [
    "best AI YouTube channels 2025",
    "顶级AI博主推荐"
  ],
  "content_queries": [
    "AI news November 2025",
    "Python教程 2025"
  ]
}
```

### 阶段 2A: 发现博主
```
web_search("best AI YouTube channels 2025")
→ 找到 12 篇文章

InfluencerExtractor
→ 提取博主:
  - AI Explained (YouTube)
  - Two Minute Papers (YouTube)
  - 李永乐老师 (Bilibili)
  - 回到2049 (Bilibili)
```

### 阶段 2A: 搜索博主内容（建立基线）
```
youtube_search "AI Explained AI"
→ 8 条视频, 平均播放量 50k

bilibili_search "李永乐老师 AI"
→ 8 条视频, 平均播放量 100k

存储: author_avg_views = {
  "AI Explained": 50000,
  "李永乐老师": 100000
}
```

### 阶段 2B: 关键词搜索
```
youtube_search "AI news November 2025"
→ 12 条视频（多个博主）

bilibili_search "AI新闻 11月"
→ 10 条视频（多个UP主）
```

### 阶段 3: 监控头部博主
```
youtube_monitor(@AIExplained)
→ 10 条最新视频

发现异常:
  - 视频A: 150k 播放 (平均 50k) → 3倍! ⭐
  - 话题: "DeepSeek V3 评测"
```

### 阶段 4: 双引擎筛选
```
引擎 1 输出 (5条):
  - AI Explained: DeepSeek V3 评测 (150k, 3x平均) ⭐⭐⭐⭐⭐
  - 李永乐老师: AI伦理问题 (200k, 2x平均) ⭐⭐⭐⭐

引擎 2 输出 (5条):
  - 某小博主: AI工具合集 (80k, 中位数10k, 8倍) ⭐⭐⭐⭐
  - 某UP主: Python异步编程 (50k, 中位数8k, 6倍) ⭐⭐⭐

合并 Top 10 → 进入 Architect
```

### 阶段 5: 选题策划
```
分析:
- 引擎 1 发现: 头部博主都在讲 "DeepSeek V3"
  → 这是一个热点话题！

- 引擎 2 发现: "AI工具合集" 这种内容形式容易爆
  → 可以做 "DeepSeek V3 工具合集"

选题方案 #1:
  标题: "DeepSeek V3 深度评测 + 实用工具推荐"
  切入点: 结合头部博主的话题 + 爆款内容形式
  来源: 引擎1 + 引擎2 混合
```

---

## ✅ 方案评估

### 优势

1. **利用现有代码** ⭐⭐⭐⭐⭐
   - Filter 已经有双引擎逻辑
   - 只需补充"发现博主"流程

2. **逻辑清晰** ⭐⭐⭐⭐⭐
   - 引擎 1: 纵向对比（头部博主）
   - 引擎 2: 横向对比（关键词搜索）
   - 两个引擎独立又互补

3. **质量保证** ⭐⭐⭐⭐⭐
   - 引擎 1 锁定顶级博主
   - 引擎 2 发现黑马内容
   - 双重保障

4. **可扩展** ⭐⭐⭐⭐
   - 博主列表可以持久化
   - 可以定期更新博主列表
   - 可以支持更多平台

### 风险

1. **Web 搜索质量依赖**
   - 如果搜不到好的推荐文章，会影响博主发现
   - 缓解：可以手动配置白名单作为兜底

2. **LLM 提取准确性**
   - 博主信息提取可能不准确
   - 缓解：使用结构化输出 + 多次验证

3. **实现复杂度**
   - 需要新增 2 个节点
   - 需要修改现有逻辑
   - 缓解：分阶段实现，逐步测试

---

## 🎯 我的建议

### 推荐：方案 A（最小改动）

**理由**：
1. 你的逻辑和现有代码高度契合
2. Filter 已经有双引擎基础
3. 只需补充"发现博主"的自动化
4. 改动可控，风险低

**实施步骤**：

1. **第一步**：创建搜索词设计节点（1小时）
2. **第二步**：创建博主提取节点（1.5小时）
3. **第三步**：修改 Planner 决策逻辑（1小时）
4. **第四步**：增强 Filter 标签（0.5小时）
5. **第五步**：测试完整流程（1小时）

**总计**：约 5 小时

---

## 🚀 下一步

我现在可以：

### 选项 1：立即实现（推荐）
- 创建 `keyword_designer.py`
- 创建 `influencer_extractor.py`
- 修改相关文件
- 完整测试

### 选项 2：先实现核心，再扩展
- 先实现搜索词设计
- 手动验证博主提取
- 确认可行后再自动化

### 选项 3：详细讨论
- 讨论更多细节
- 确认实现方案
- 再开始编码

---

你觉得怎么样？我的建议是**选项 1**，直接实现完整方案，因为：
1. 你的逻辑非常清晰
2. 现有代码基础很好
3. 改动量可控

需要我现在开始实现吗？🚀
